Implementation
==============

Accml assumes that different views
are used on interacting with the
same particle accelerator: the  *design*
and *device* view. A client interacts with
accml using clearly structured messages. This design is
discussed in more detail in :doc:`design`.

Communication core
------------------
accml focuses on interacting with an particle accelerator,
its simulation tool or its twin in a structured manner.

